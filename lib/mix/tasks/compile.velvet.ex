defmodule Mix.Tasks.Compile.Velvet do
  @shortdoc "Compile .vel sources to elixir"
  @moduledoc """
  Compile Velvet sources into elixir.

  Make sure to add the :velvet compiler before
  :elixir in your `mix.exs` `compilers` option.
  """

  use Mix.Task

  def run(_args) do
  end
end
